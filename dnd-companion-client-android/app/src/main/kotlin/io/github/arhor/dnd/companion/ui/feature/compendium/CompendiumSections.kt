package io.github.arhor.dnd.companion.ui.feature.compendium

enum class CompendiumSections(
    val title: String,
) {
    SPELLS(title = "Spells"),
    CONDITIONS(title = "Conditions"),
    ALIGNMENTS(title = "Alignments"),
    RACES(title = "Races"),
}
